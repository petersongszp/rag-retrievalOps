# L7 - 离线评测、回归门禁与贡献度分析 测试报告

## 测试目标
检查评测脚本是否存在，评测数据集是否完整，验证 Go retrieval-eval 入口，检查评测数据集内容。

## 测试环境
- 测试时间：2026-05-26 01:57 CST（回归测试）
- API 地址：http://localhost:8899
- 评测脚本路径：backend/scripts/evaluation/
- Go 入口路径：backend/cmd/retrieval-eval/main.go

## 测试用例

### 用例1: 评测脚本存在性
- **测试方法**：检查 `scripts/evaluation/` 目录下的文件列表
- **预期结果**：包含 evaluate.py、evaluate_trulens.py 等脚本
- **实际结果**：
  - `dataset.json` ✅ — 评测数据集
  - `evaluate.py` ✅ — 主评测入口（支持 retrieval 和 ragas 两种模式）
  - `evaluate_trulens.py` ✅ — TruLens 评测脚本
  - `index_benchmark_dataset.example.json` ✅ — 索引基准数据集示例
  - `index_scan_profiles.json` ✅ — 索引扫描配置
  - `README.md` ✅ — 使用说明
  - `requirements.txt` ✅ — Python 依赖
  - `retrieval_gate_thresholds.example.json` ✅ — 回归门禁阈值配置
  - `retrieval_strategy_profiles.example.json` ✅ — 检索策略配置示例
  - `trulens_report.json` ✅ — TruLens 报告
- **测试结论**：✅ 通过

### 用例2: Go retrieval-eval 入口
- **测试方法**：检查 `backend/cmd/retrieval-eval/main.go` 是否存在
- **预期结果**：文件存在
- **实际结果**：`True` ✅
- **测试结论**：✅ 通过

### 用例3: 评测数据集内容（已更新）
- **测试方法**：读取 `scripts/evaluation/dataset.json`
- **预期结果**：包含实际测试数据（Go/React/JavaScript 相关 query 和真实 chunk_id）
- **实际结果**：数据集包含实际测试数据：
  1. `entity-go-goroutine` — Go goroutine 查询，`relevant_ids: ["kb_1_doc_1_chunk_0_1779523924191899824"]` ✅
  2. `entity-react-component` — React 组件查询，`relevant_ids: ["kb_1_doc_2_chunk_0_1779523936358216873"]` ✅
  3. `entity-js-promise` — JavaScript Promise 查询，`relevant_ids: ["kb_1_doc_4_chunk_0_1779731716268688925"]` ✅
  4. `abbreviation-gc` — gc 缩写查询 ✅
  - 每条包含：id, question, query, context, ground_truth, top_k, relevant_ids, citation_targets, query_type, tags
  - **relevant_ids 已从占位符更新为真实 chunk_id** ✅
- **测试结论**：✅ 通过

### 用例4: 评测脚本功能（代码审查）
- **测试方法**：审查 `evaluate.py` 的主要功能
- **预期结果**：支持 retrieval 回归测试和 ragas 问答评测
- **实际结果**：
  - 支持 `--mode retrieval` 和 `--mode ragas` 两种模式
  - retrieval 模式：调用 `/api/admin/kb/retrieve` API，计算 Recall@K/MRR/nDCG
  - ragas 模式：使用 ragas 库评估 answer_relevancy/context_recall/faithfulness
  - 支持 `--candidate` 参数指定候选策略名称
  - 输出到 `docs/retrieval-regression-report-{candidate}.json`
- **测试结论**：✅ 通过

### 用例5: 门禁阈值配置
- **测试方法**：读取 `retrieval_gate_thresholds.example.json`
- **预期结果**：包含合理的回归门禁阈值
- **实际结果**：
  - `min_recall_delta: 0.08` — Phase 2 相比 Phase 1 召回率至少提升 8%
  - `max_p95_latency_regression_ratio: 0.2` — P95 延迟回归不超过 20%
  - 其他指标（MRR/nDCG/citation_accuracy）delta 为 0.0
- **测试结论**：✅ 通过

### 用例6: 评测脚本可执行性检查
- **测试方法**：检查 `requirements.txt` 依赖
- **预期结果**：包含必要的 Python 依赖
- **实际结果**：
  - 依赖文件存在 ✅
  - 需要安装：requests, datasets, ragas 等
  - 当前环境未安装 Python 依赖，无法直接运行
  - 需要先 `pip install -r requirements.txt`
- **测试结论**：⚠️ 部分通过（脚本存在但依赖未安装，无法实际运行）

## 测试总结
- **通过率**：5/6 (83%)，1 个部分通过
- **修复说明**：
  - ✅ 评测数据集已从占位符更新为实际测试数据
  - ✅ relevant_ids 使用真实的 chunk_id（如 `kb_1_doc_1_chunk_0_1779523924191899824`）
  - ✅ Go retrieval-eval 入口 `backend/cmd/retrieval-eval/main.go` 存在
- **待改进**：
  1. Python 依赖未安装，无法直接运行评测脚本
  2. 建议在 Docker 容器中预装 Python 依赖，或提供 Docker 化的评测环境

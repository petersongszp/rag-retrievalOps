# L6 - 索引参数优化与基准报告 测试报告

## 测试目标
检查 Milvus collection 的索引参数配置，验证参数扫描脚本是否存在且可执行。

## 测试环境
- 测试时间：2026-05-26 01:32 CST
- API 地址：http://localhost:8899
- Milvus：milvus-standalone (healthy)
- 测试脚本路径：backend/scripts/evaluation/

## 测试用例

### 用例1: Index Scan Profiles 配置文件
- **测试方法**：读取 `scripts/evaluation/index_scan_profiles.json`
- **预期结果**：包含多个索引配置 profile（HNSW/IVF 等）
- **实际结果**：文件存在，包含 4 个 profile：
  1. **phase1-hnsw-baseline**：HNSW, m=16, ef_construction=200, ef_search=64（基线）
  2. **phase2-hnsw-balanced**：HNSW, m=24, ef_construction=320, ef_search=96（平衡）
  3. **phase2-hnsw-high-recall**：HNSW, m=32, ef_construction=360, ef_search=128（高召回）
  4. **phase2-ivf-balanced**：IVF_FLAT, nlist=2048, nprobe=32（低内存）
- **测试结论**：✅ 通过

### 用例2: Milvus Collection 索引参数（代码审查）
- **测试方法**：审查 `config.yaml` 中的 Milvus 配置
- **预期结果**：包含 MetricType、SearchTimeout 等配置
- **实际结果**：
  - `MetricType: 'COSINE'` ✅
  - `SearchTimeout: 30s` ✅
  - `ConnectTimeout: 10s` ✅
  - `CollectionName: 'documents'` ✅
- **测试结论**：✅ 通过

### 用例3: 参数扫描脚本完整性
- **测试方法**：检查 `scripts/evaluation/` 目录下的相关文件
- **预期结果**：包含参数扫描相关的配置和脚本
- **实际结果**：
  - `index_scan_profiles.json` ✅ — 4 个索引参数配置
  - `index_benchmark_dataset.example.json` ✅ — 基准数据集模板
  - `evaluate.py` ✅ — 评测入口脚本
  - `README.md` ✅ — 使用说明
- **测试结论**：✅ 通过

### 用例4: Retrieval Strategy Profiles
- **测试方法**：读取 `scripts/evaluation/retrieval_strategy_profiles.example.json`
- **预期结果**：包含多个检索策略配置
- **实际结果**：4 个策略 profile：
  1. **dense_only**：基线 Dense 检索
  2. **hybrid**：混合检索, candidate_top_k=10
  3. **hybrid_rewrite**：混合+改写, candidate_top_k=10
  4. **hybrid_rewrite_dynamic_topk**：混合+改写+动态TopK（完整 Phase 2）
- **测试结论**：✅ 通过

### 用例5: Retrieval Gate Thresholds
- **测试方法**：读取 `scripts/evaluation/retrieval_gate_thresholds.example.json`
- **预期结果**：包含回归门禁阈值
- **实际结果**：
  - `min_recall_delta: 0.08` — 召回率最小提升 8%
  - `min_mrr_delta: 0.0`
  - `min_ndcg_delta: 0.0`
  - `min_citation_accuracy_delta: 0.0`
  - `max_p95_latency_regression_ratio: 0.2` — P95 延迟回归不超过 20%
- **测试结论**：✅ 通过

## 测试总结
- **通过率**：5/5 (100%)
- **发现的问题**：无
- **建议改进方向**：
  1. 可增加自动化索引参数扫描脚本，批量运行不同 profile 并对比结果
  2. 建议在基准数据集模板中填充实际的测试数据，而非使用 example 文件

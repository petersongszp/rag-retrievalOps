# L0 - 策略开关、配置冻结与基线快照 测试报告

## 测试目标
验证 Phase 2 feature flags 可独立开关，配置校验正确（权重和为1.0、min_topk < max_topk 等），启动日志包含完整策略快照。

## 测试环境
- 测试时间：2026-05-26 01:28 CST
- API 地址：http://localhost:8899
- 测试数据：知识库 id=1（Go/React 教程），id=2（L6 冒烟测试）
- Docker 容器：app-backend (healthy), app-mysql, app-redis, milvus-standalone

## 测试用例

### 用例1: Feature Flags 独立开关验证
- **测试方法**：读取 config.yaml 中的 `rag.feature_flags` 配置，检查四个 Phase 2 开关的状态
- **预期结果**：所有四个开关均为 `true`
- **实际结果**：
  - `enable_hybrid_retrieval: true` ✅
  - `enable_query_rewrite: true` ✅
  - `enable_dynamic_topk: true` ✅
  - `enable_advanced_rerank: true` ✅
- **测试结论**：✅ 通过

### 用例2: 配置校验 - 权重和为1.0
- **测试方法**：读取 `rag.phase2.hybrid_dense_weight` 和 `hybrid_sparse_weight`，计算总和
- **预期结果**：dense_weight + sparse_weight = 1.0
- **实际结果**：
  - `hybrid_dense_weight: 0.7`
  - `hybrid_sparse_weight: 0.3`
  - `weight_sum: 1.0` ✅
- **测试结论**：✅ 通过

### 用例3: 配置校验 - min_topk < max_topk
- **测试方法**：检查 `rag.phase2` 中的 `min_topk` 和 `max_topk` 配置
- **预期结果**：min_topk < max_topk
- **实际结果**：
  - `min_topk: 3`
  - `max_topk: 8`
  - `3 < 8` ✅
- **测试结论**：✅ 通过

### 用例4: 启动日志包含完整策略快照
- **测试方法**：通过 `docker logs app-backend` 检查启动日志中的 `[RAG:L0]` 标记
- **预期结果**：日志包含 snapshot version、strategy_digest、flags、thresholds、phase2 参数
- **实际结果**：
```
[RAG:L0] phase1 baseline snapshot created: /app/docs/baseline/phase1/baseline_snapshot.json
[RAG:L0] snapshot version=cf5434331e2a41df strategy_digest=1f179aa1b00651fa env=dev enabled=true
  flags={prod_guard:false ingest_retry:false retrieve_audit:true hybrid:true rewrite:true dynamic_topk:true adv_rerank:true}
  thresholds={max_retry_count:3 retry_backoff_ms:500 retrieve_timeout_ms:3000 user_qps_limit:20}
  phase2={hybrid_dense_weight:0.700 hybrid_sparse_weight:0.300 candidate_topk:10 min_topk:3 max_topk:8 token_budget:0 min_answer_chunks:2 rewrite_timeout_ms:120 rewrite_max_expansions:3 rerank_timeout_ms:250 rerank_model:jaccard-v1}
  release={enabled:false stage:full internal_roles:admin canary_percent:5 batch_percent:25 allowlist_count:0}
```
- **测试结论**：✅ 通过

### 用例5: 启动日志包含 Phase 2 参数确认
- **测试方法**：检查日志中的 `[RAG:L0] phase2_flags` 和 `[RAG:L0] phase2_params`
- **预期结果**：日志明确记录所有 Phase 2 开关和参数
- **实际结果**：
```
[RAG:L0] phase2_flags hybrid=true rewrite=true dynamic_topk=true advanced_rerank=true
[RAG:L0] phase2_params dense_weight=0.700 sparse_weight=0.300 candidate_topk=10 min_topk=3 max_topk=8 token_budget=0 min_answer_chunks:2 rewrite_timeout_ms=120 rewrite_max_expansions:3 rerank_timeout_ms:250 rerank_model=jaccard-v1
```
- **测试结论**：✅ 通过

## 测试总结
- **通过率**：5/5 (100%)
- **发现的问题**：无
- **建议改进方向**：
  1. 可考虑在启动时增加配置校验失败的断言（如权重和不为1.0时拒绝启动）
  2. 可增加配置版本号的 hash 校验，防止运行时配置被篡改
